# CLAUDE.md — מסמך ראובן

## Overview
קובץ ההוראות הראשי של הפרויקט, נטען אוטומטית לכל סשן של Claude Code. מגדיר ש-Claude משחק את **ראובן — מנכ"ל הצוות**: מקבל בקשות מהמשתמש, מבין מה לעשות, ומחליט את מי מאנשי הצוות (יעל, יובל, חן) להפעיל. גם מתאר את מבנה הפרויקט (`.claude/agents/`, `.claude/skills/`, `.claude/commands/`).

## File path
`CLAUDE.md`

## Belongs to
**ראובן** — זהו "המנדט" של ראובן כמנכ"ל. שאר הצוות מקבל מאמרים נפרדים תחת `.claude/agents/`.

## What it currently defines
- זהות ראובן ותפקידו (מתאם / מנתב משימות)
- שמות אנשי הצוות: [[yael]] (כתיבה), [[yuval]] (עיצוב/תמונות), [[chen]] (מחקר)
- מבנה התיקיות: `.claude/agents/`, `.claude/skills/`, `.claude/commands/`

## Status
שלד ראשוני. הקובץ עצמו מציין: "זהו קובץ ראשוני... בהמשך נוסיף הוראות ניתוב מדויקות וכללי עבודה מול הצוות".

## Related
- [[reuven]] — דמות המנכ"ל שמוגדרת כאן
- [[yael]], [[yuval]], [[chen]] — חברי הצוות שראובן מנהל
- [[obsidian-vault-workflow]] — פרוטוקול חובה שמופעל לפני כל פעולה
- [[env-example]] — מפתחות ה-API שאנשי הצוות צריכים
